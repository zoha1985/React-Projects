import React from 'react'
import Card from '../ui/card'
import Logo from '../logo'
import NavBar from '../navbar'

/**
* @author
* @function Hero
**/

const Hero = (props) => {
  return(
    <div>

        <Card>
        <Logo/>
        <NavBar/>
        </Card>
    </div>
   )

 }

export default Hero